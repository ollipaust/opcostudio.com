import styled from 'styled-components'
import { accent, light, dark, darker } from 'constants/colors'

export const Container = styled.div`
  padding: 0 4rem;
  margin: 0 4rem;
  height: 90vh;
  border-radius: 10px;
  position: relative;
  text-align: right;
`
